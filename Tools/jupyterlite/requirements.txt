jupyterlite==0.8.1
jupyterlite-core==0.8.1
jupyterlite-pyodide-kernel==0.8.1
# jupyter-iframe-commands — labextension running INSIDE the editor iframe that
# exposes the JupyterLab command registry to the parent frame over postMessage.
# Paired with the vendored jupyter-iframe-commands-host bridge (Public/vendor/),
# this replaces notebook.js's fragile `frame.contentWindow.jupyterapp.commands`
# cross-frame poking with a supported API.  Federated into the bundle by
# `jupyter lite build`; do not add to litePluginSettings (auto-discovered).
jupyter-iframe-commands==0.3.0
# BOTH editor kernels — xpython (xeus-python) and xr (xeus-r) — are built from
# emscripten-forge, one env each: Tools/jupyterlite/environment-python.yml and
# environment-r.yml.  The build needs micromamba on PATH and network to
# repo.prefix.dev.  R is on xeus rather than
# WebR because jupyterlite-webr is dead-ended at jupyterlite-core<0.7 (see
# docs/xeus-r-kernel-spike.md, #77).
jupyterlite-xeus==5.0.0
# The Pyodide kernel is no longer the editor's Python (defaultKernelName is
# xpython).  It stays vendored on purpose: scripts/check-pyodide-parity.sh
# derives the Public/pyodide version pin from this package's bundled core
# wheels, and keeping it is the cheap revert if xeus-python has to be backed
# out.  Public/pyodide itself is still load-bearing for browser grading and
# /validate, which are not xeus.
