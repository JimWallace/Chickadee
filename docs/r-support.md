# R support

Chickadee grades and personalizes assignments authored in **R** as well as
Python. The grading path has always been language-agnostic (test suites are
shell/`.R`/`.py` scripts run by exit code); this document covers the pieces that
became language-aware so that **per-student personalization works fully in R**,
transparently to an instructor who never thinks about what runs under the hood.

## Language identity

An assignment's language is a first-class value, `AssignmentLanguage`
(`Sources/Core/AssignmentLanguage.swift`), resolved from the manifest:

1. any `.R`/`.r` graded test script → `.r` (the graded suite is the strongest
   signal — it is what actually runs);
2. else an R notebook kernel (`kernelspec.name` in `{ir, r, webr, xr}` or
   `language_info.name == "r"`) → `.r`;
3. else `.python` — the default, so every existing assignment resolves exactly
   as before and its generated bytes stay byte-for-byte identical.

`AssignmentLanguage.resolve(manifest:notebookKernelName:notebookLanguageInfoName:)`
is the single resolver. There is no persisted language column yet — a
first-class field is a clean, migration-only follow-up, not required, because
the manifest already determines the answer for everything that ships.

The enum owns its own language-specific behaviour (a closed 2-case enum rather
than a protocol + conformances):

- `literal(_ value: JSONValue) -> String` — `pythonLiteral` vs `rLiteral`.
- `inputsFileName` — `_ck_inputs.py` vs `_ck_inputs.R`.
- `renderInputsFile(_:)` — the per-student grading-inputs file body (see below).

Adding a third language is one `case` and its literal renderer.

## Per-student personalization in R

The three personalization channels are now language-parameterized. The default
is `.python` at every call site, so the Python paths are unchanged.

### 1. Seed (already language-agnostic)

The per-`(student, assignment)` seed rides an env var,
`CHICKADEE_ASSIGNMENT_SEED` (64 hex chars / 256 bits) — any interpreter reads
it. Python binds `seed = int(hex, 16)` (a 256-bit int). Base R has no bignum, so
`RPersonalizationRuntime.chickadeeSeedRSource` provides `chickadee_seed()`: a
deterministic Horner-fold of the hex digits modulo `2^31 - 1` (every
intermediate stays below `2^35`, safely inside a double), yielding an R integer.
Python and R assignments are **distinct** assignments, so only *within-R*
consistency is required — and it holds because the server-side expression driver
and the grading runtime compute `chickadee_seed()` from the *same Core source*.

### 2. `=` expressions — evaluated per-language on the server

Instructor `=` expressions are evaluated by `PersonalizationEvaluator`
(`Sources/APIServer/Services/`), which spawns the assignment's interpreter
against a generated driver: `python3` for Python, **`Rscript` for R**. The R
driver binds `seed <- chickadee_seed()`, `source()`s each `.R` support file
(including the extracted `solution.R`), binds every static variable via
`rLiteral`, evaluates each `= expression` in declared order, and emits — as the
**last** stdout line — a JSON map `name → deparse(value)` (an R-literal string
per value). The Swift return path parses that last line identically for both
languages; only the driver bytes differ.

This preserves the deliberate property that **expression source and the
reference solution never reach the runner** (`TestProperties.runnerSanitized()`
still strips expressions from the `Job`). The accepted cost is `r-base` on the
`chickadee-server` image — already present in the `Dockerfile` (the single final
image carries both `python3` and `r-base` plus both binaries), so `Rscript` is
available server-side with no change.

The resolved values are delivered to grading as **`_ck_inputs.R`**, written by
the worker into the grading workspace:

```r
# Auto-generated per-student grading inputs (issue #461). Do not edit.
.ck_inputs <- list(
    `sample1_reads` = c("AACGT", "GGTTA")
)
```

(`.ck_inputs`, not `_ck` — R forbids a leading-underscore identifier — and R's
`list()` rejects a trailing comma, so the R form omits it.) A grading script
reads a value with `chickadee_inputs()[["sample1_reads"]]`
(`RPersonalizationRuntime.chickadeeInputsRSource`, the R mirror of a Python test
reading `_ck["name"]`). The generated-file bytes are byte-identical across
students; only the resolved-values map differs.

### 3. `{{name}}` notebook substitution — per-language literals

`PersonalizationSubstitution.resolve(..., language:)` renders static variables
via `language.literal` and pulls expression values from the per-language driver,
so an R notebook's `{{sample1_reads}}` becomes `c("AACGT", "GGTTA")` (valid R),
not a Python list. `NotebookSubstitution.apply` itself is a dumb string
substituter — it needs no language knowledge because it receives already-rendered
literals.

## The R grading runtime

`Tools/runner-support/test_runtime.R` (mirrored inline as `testRuntimeR` in
`Sources/Worker/TestRuntimeSources.swift`) is the helper library injected into
every R test working directory:

- `passed()` / `failed()` / `errored()` — exit 0/1/2, hand-formatted JSON so
  there is no `jsonlite` dependency;
- `chickadee_seed()` / `chickadee_inputs()` — the personalization primitives;
- `chickadee_student_file()` / `chickadee_load_student()` /
  `chickadee_require_fn()` — locating and loading the student's submission.

The worker's `testRuntimeR` is *composed* from the one Core source
(`RPersonalizationRuntime`) rather than re-pasting the R, which is what
guarantees the grading runtime and the server driver never drift on the seed.
`RuntimeSourceDriftTests` pins the inline copy against the canonical file.

### Locating the submission (and why it is central)

`chickadee_student_file(extra_skip)` is the R mirror of `test_runtime.py`'s
`_preferred_student_module()` / `_candidate_student_files()`. Python reserves
its own workspace filenames *inside the runtime*; R originally had no
equivalent, so every assignment's helper hand-rolled the search — and each one
had to remember, on its own, that `_ck_inputs.R` is an R file sitting right
next to the submission. A helper that simply scanned `*.R` could grade
Chickadee's per-student inputs as the student's code.

The reservation is now real: `.chickadee_reserved_files` is built in Swift with
the inputs filename interpolated straight from `AssignmentLanguage`, so the skip
list cannot drift from the file the worker actually writes. Resolution order is

1. the runner's `.chickadee_student_module` hint, when it names an `.R` file
   that is actually present (this disambiguates a student who uploaded a stray
   `.R` beside their notebook);
2. otherwise a scan of `*.R`, skipping reserved files, `*test_*` scripts, and
   any `extra_skip` the assignment passes for its own bundled helpers;
3. `solution.R` wins when present — the validation path.

A stale hint never wins: it must name an existing `.R` file or the scan takes
over. That matters because the hint is produced by
`legacyPreferredStudentModuleFilename`, which extracts a notebook to its
assignment's source language — an R job's `analysis.ipynb` becomes `analysis.R`,
not `analysis.py`.

## What is deliberately not (yet) in R

- **Literal globals inlined into hand-authored `.R` test scripts.** For Python,
  `TestScriptVariablePrepender` prepends `name = <pythonLiteral>` to raw `.py`
  scripts at save time; it is guarded by `.hasSuffix(".py")`, so `.R` scripts
  pass through untouched (no breakage). R tests instead read per-student values
  from `chickadee_inputs()` or reference the values delivered by notebook
  substitution. Routing literal globals through the uniform `_ck_inputs.R`
  channel is a clean follow-up; it is not needed for the assignments that ship
  today (their tests read `chickadee_inputs()`).
- **Pattern families and notebook checks in R.** Their renderers emit Python.
  R assignments hand-author `.R` tests today; per-language renderers are a
  separate follow-up (they do not block the personalization engine).

## Verification

- `Tests/CoreTests/JSONValueRLiteralTests.swift`,
  `AssignmentLanguageTests.swift`, `AssignmentLanguageStrategyTests.swift` —
  `rLiteral`, resolution, the strategy methods, and the byte-exact
  `_ck_inputs.{py,R}` bodies.
- `Tests/APITests/PersonalizationEvaluatorRTests.swift` — the R driver's byte
  shape (CI-safe) and a real `Rscript` round-trip (seed math, `rLiteral`
  binding, `deparse` → JSON → decode). The round-trip is silently skipped where
  `Rscript` is absent; it runs in the r-base container and locally.
