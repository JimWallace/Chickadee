#extend("base"):
#export("title"):
Create Assignment
#endexport
#export("content"):
#if(error):
<div class="error-box">
    <h2>Could not create assignment</h2>
    <p class="card-meta error-detail">#(error)</p>
</div>
#endif


<form class="form form--wide" method="post" action="/instructor/new/save" enctype="multipart/form-data" id="new-assignment-form" novalidate>
    #csrfFormField()
    #if(draftID):
    <input type="hidden" name="draftID" value="#(draftID)">
    #endif

    <!-- ── Top header row ─────────────────────────────────────────────── -->
    <div class="editor-titlebar">
        <div class="editor-title-col">
            <label class="visually-hidden" for="assignmentNameInput">Assignment Name</label>
            <input class="form-input assignment-name-input" type="text" name="assignmentName" id="assignmentNameInput" required
                   value="#(assignmentName)"
                   placeholder="Assignment name">
            <div class="assignment-meta-row">
                <label class="field-inline">
                    Opens
                    <input class="form-input input-compact" type="datetime-local" name="startsAt" id="startsAt" value="#(startsAt)">
                </label>
                <label class="field-inline">
                    Due
                    <input class="form-input input-compact" type="datetime-local" name="dueAt" id="dueAt" value="#(dueAt)">
                </label>
                #if(!sections.isEmpty):
                <label class="field-inline">
                    Section
                    <select class="form-input input-compact" name="sectionID" id="sectionID">
                        <option value="" #if(!preselectedSectionID):selected#endif>— Ungrouped —</option>
                        #for(section in sections):
                        <option value="#(section.sectionID)"
                                data-grading-mode="#(section.defaultGradingMode)"
                                #if(preselectedSectionID == section.sectionID):selected#endif>#(section.name)</option>
                        #endfor
                    </select>
                </label>
                #endif
                <label class="field-inline">
                    Language
                    <select class="form-input input-compact" name="assignmentLanguage"
                            id="assignmentLanguageSelect" required>
                        #for(option in assignmentLanguageOptions):
                        <option value="#(option.value)" #if(option.selected):selected#endif>#(option.label)</option>
                        #endfor
                    </select>
                </label>
            </div>
            <p class="card-meta fine-print">Every generated test renders in this language, so it is stated here rather than worked out later. Pick "None" for a suite of plain shell scripts. Choosing #(uploadOnlyLanguagesProse) makes the assignment upload-only, since those languages have no notebook editor.</p>
            <div id="uw-date-warning" class="card-meta date-warning" style="display:none"></div>
            <p id="grading-mode-hint" class="card-meta grading-mode-hint" style="display:none"></p>
        </div>
        <div class="editor-actions">
            <button class="btn btn-primary" type="submit">Create &amp; Validate</button>
            <a class="btn" href="/instructor">Cancel</a>
        </div>
    </div>

    <!-- ── Notebooks ──────────────────────────────────────────────────── -->

    <!-- Hidden file inputs triggered by JS buttons -->
    <input type="file" id="assignment-notebook-file-input" name="assignmentNotebookFile" accept=".ipynb" hidden>
    <input type="file" id="solution-notebook-file-input"   name="solutionNotebookFile"   accept=".ipynb" hidden>

    <!-- Hidden submit buttons for draft notebook actions, fired by JS -->
    <button id="js-upload-assignment-notebook"  type="submit" formaction="/instructor/new/draft" name="draftAction" value="upload-assignment-notebook"  hidden></button>
    <button id="js-upload-solution-notebook"    type="submit" formaction="/instructor/new/draft" name="draftAction" value="upload-solution-notebook"    hidden></button>
    <button id="js-create-assignment-notebook"  type="submit" formaction="/instructor/new/draft" name="draftAction" value="create-assignment-notebook"  hidden></button>
    <button id="js-create-solution-notebook"    type="submit" formaction="/instructor/new/draft" name="draftAction" value="create-solution-notebook"    hidden></button>
    <button id="js-create-solution-from-assignment" type="submit" formaction="/instructor/new/draft" name="draftAction" value="create-solution-from-assignment" hidden></button>
    <button id="js-clear-assignment-notebook"   type="submit" formaction="/instructor/new/draft" name="draftAction" value="clear-assignment-notebook"   hidden></button>
    <button id="js-clear-solution-notebook"     type="submit" formaction="/instructor/new/draft" name="draftAction" value="clear-solution-notebook"     hidden></button>

    <div class="editor-block">
        <div class="editor-block-head">
            <h2>Files</h2>
            #if(draftID):
            <div class="toolbar toolbar--end">
                <button class="btn action-btn" type="button" id="add-support-file-btn"
                        title="Upload a data file (CSV, JSON, etc.) bundled with the assignment">+ Add Support File</button>
                <input type="file" id="support-file-upload-input" hidden multiple>
                <span id="support-file-upload-status" class="upload-status-inline" role="status" aria-live="polite"></span>
            </div>
            #endif
        </div>
        <table class="results-table" id="notebook-files-table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>File</th>
                    <th class="time">Action</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><strong>Assignment Notebook</strong></td>
                    <td>
                        #if(assignmentNotebook):
                        <a href="#(assignmentNotebook.editURL)"><code id="assignment-notebook-name">#(assignmentNotebook.name)</code></a>
                        #else:
                        <code class="card-meta">— not set —</code>
                        #endif
                    </td>
                    <td class="time">
                        <div class="cell-actions">
                            #if(assignmentNotebook):
                            <a class="btn action-btn" href="#(assignmentNotebook.editURL)">Edit</a>
                            <button class="btn action-btn action-danger" type="button"
                                    onclick="document.getElementById('js-clear-assignment-notebook').click()">Clear</button>
                            #else:
                            <button class="btn action-btn" type="button" id="upload-assignment-notebook-btn">Upload</button>
                            <button class="btn action-btn" type="button"
                                    onclick="document.getElementById('js-create-assignment-notebook').click()">Create Blank</button>
                            #endif
                        </div>
                    </td>
                </tr>
                <tr>
                    <td><strong>Solution Notebook</strong></td>
                    <td>
                        #if(solutionNotebook):
                        <a href="#(solutionNotebook.editURL)"><code id="solution-notebook-name">#(solutionNotebook.name)</code></a>
                        #else:
                        <code class="card-meta">— not set —</code>
                        #endif
                    </td>
                    <td class="time">
                        <div class="cell-actions">
                            #if(solutionNotebook):
                            <a class="btn action-btn" href="#(solutionNotebook.editURL)">Edit</a>
                            <button class="btn action-btn action-danger" type="button"
                                    onclick="document.getElementById('js-clear-solution-notebook').click()">Clear</button>
                            #else:
                            <button class="btn action-btn" type="button" id="upload-solution-notebook-btn">Upload</button>
                            <button class="btn action-btn" type="button"
                                    onclick="document.getElementById('js-create-solution-notebook').click()">Create Blank</button>
                            #if(assignmentNotebook):
                            <!-- Parity PR 4 of #433: only meaningful once an
                                 assignment notebook exists; copies its bytes
                                 into the draft solution path. -->
                            <button class="btn action-btn" type="button"
                                    title="Copy the assignment notebook as a starting point for the solution"
                                    onclick="document.getElementById('js-create-solution-from-assignment').click()">Create from Assignment</button>
                            #endif
                            #endif
                        </div>
                    </td>
                </tr>
                <!-- Support files: data fixtures (CSVs, JSON, etc.) bundled
                     with the assignment.  Anything in the draft zip that
                     isn't a test script and isn't a notebook lands here.
                     v0.4.132 / parity PR 3 of #433. -->
                #for(supportFile in supportFileRows):
                <tr data-support-file="#(supportFile.name)">
                    <td><strong>Support file</strong></td>
                    <td>
                        <a href="#(supportFile.url)"><code>#(supportFile.name)</code></a>
                    </td>
                    <td class="time">
                        <button class="btn action-btn action-danger js-support-file-delete-btn" type="button"
                                data-filename="#(supportFile.name)">Remove</button>
                    </td>
                </tr>
                #endfor
            </tbody>
        </table>
    </div>

    <!-- ── Runner Requirements ────────────────────────────────────────── -->
    #if(!detectedLanguages.isEmpty):
    <p class="card-meta detected-note">
        <strong>Auto-detected:</strong>
        Languages: #(detectedLanguagesCSV)
        #if(!detectedCapabilities.isEmpty):
        · Capabilities: #(detectedCapabilitiesCSV)
        #endif
    </p>
    #endif
    <div class="runner-req-row">
        <label class="req-field">
            Languages
            <input class="form-input input-sm" type="text" name="requiredLanguagesCSV" value="#(requiredLanguagesCSV)"
                   placeholder="e.g. #(languageTokensCSV)">
        </label>
        <label class="req-field">
            Capabilities
            <input class="form-input input-sm" type="text" name="requiredCapabilitiesCSV" value="#(requiredCapabilitiesCSV)"
                   placeholder="numpy, pandas, shell-bash">
        </label>
    </div>

</form>

<!-- Test Suite editor lives OUTSIDE the Save & Validate multipart form
     so its nested per-op <form> elements (Add Section, rename section,
     delete section) parse correctly.  The browser strips nested <form>
     tags, which is why the v0.4.96 in-form `+ Section` JS button
     worked but a server-rendered `+ Section` form would not.  Suite
     edits go through dedicated draft-scoped endpoints (JS PUT
     /instructor/new/draft/suite for items, POST /instructor/new/draft
     /suite-sections/* for section CRUD) — none of them rely on the
     outer form. -->

#if(draftID):
<label class="visually-hidden" for="suite-files-input">Add test suite files</label>
<input class="form-input" id="suite-files-input" type="file" multiple hidden>

<div class="form form--wide suite-editor-section">
    <div class="editor-block-head">
        <h2>Test Suite</h2>
        <div class="toolbar toolbar--end">
            <!-- Add Section — form POST + full page reload, mirrors the
                 edit-page pattern (assignment-edit.leaf:154-168). -->
            <details class="add-section-details popup-anchor" id="add-suite-section-details">
                <summary class="btn action-btn summary-btn">+ Section</summary>
                <div class="add-section-popup card suite-section-popup">
                    <form method="post" action="/instructor/new/draft/suite-sections?draftID=#(draftID)" class="form popup-form">
                        #csrfFormField()
                        <label class="form-label">
                            Section name
                            <input class="form-input editor-input" type="text" name="name" placeholder="e.g. Question 1" required>
                        </label>
                        <button class="btn btn-primary btn-compact" type="submit">Create section</button>
                    </form>
                </div>
            </details>
            <!-- Test authoring is per-section now (each section's inline
                 "+ Add Test" opens the unified Test Editor modal).  The hidden
                 Upload button is kept as the file-input trigger wired by
                 suite-table.js (`#add-test-btn`). -->
            <button class="btn action-btn" type="button" id="add-test-btn" hidden>Upload</button>
        </div>
    </div>

    <!-- Generate Tests from Solution (hidden; shown when solution notebook present) -->
    <div id="gen-tests-panel" class="gen-tests-panel" style="display:none">
        <div class="gen-tests-row">
            <span class="gen-tests-title">Generate Starter Tests</span>
            <button class="btn btn-sm" type="button" id="scan-solution-btn">Scan Solution for Functions</button>
            <span id="scan-status" class="editor-status" role="status" aria-live="polite"></span>
        </div>
        <div id="scan-results" style="display:none">
            <p class="card-meta gen-tests-prompt">Select the functions to generate starter tests for:</p>
            <div id="fn-checklist" class="fn-checklist"></div>
            <div class="gen-template-row">
                <label class="gen-template-label">
                    Template:
                    <select id="gen-template-select" class="form-input select-xs">
                        <option value="py:differential" selected>Differential (reference solution)</option>
                    </select>
                </label>
            </div>
            <button class="btn btn-primary btn-compact" type="button" id="add-gen-tests-btn">Add Selected Tests to Suite</button>
        </div>
    </div>

    <!-- Server-rendered section shells (parity with assignment-edit.leaf,
         #435 / v0.4.132).  Each .section-block owns its own table shell;
         `suite-table.js` populates the `<tbody data-section-id>` rows from
         the JSON seed and keeps them in sync via the debounced
         `PUT /instructor/new/draft/suite`.  Section CRUD goes through the
         draft-scoped endpoints in AssignmentRoutes+DraftSections.swift. -->
    #extend("_suite-sections")
</div>

<!-- Suite seed (full reconciled GET /suite payload) — same shape the
     edit page emits.  rawJSON emits the string verbatim so JSON.parse
     in suite-table.js doesn't choke on HTML-escaped quotes. -->
<script id="suite-state-seed" type="application/json">#rawJSON(suiteStateJSON)</script>
#else:
<!-- Pre-draft state: nothing to author yet.  The notebook upload above
     creates the draft setup, after which the page reloads with the
     suite editor visible. -->
<div class="predraft-note">
    <strong>Test Suite</strong> — upload an assignment notebook above to begin authoring tests.
</div>
#endif

<!-- Pattern families seed — same shape as the edit page's seed.
     Server renders the current draft's families (empty before first save). -->
<script id="pattern-families-seed" type="application/json">#rawJSON(patternFamiliesJSON)</script>
<!-- Notebook checks seed (parity PR 2 of #433).  Empty `[]` until a draft
     exists; the check editor module silently no-ops in that case. -->
<script id="notebook-checks-seed" type="application/json">#rawJSON(notebookChecksJSON)</script>
<script id="check-schema" type="application/json">#rawJSON(checkSchemaJSON)</script>
<script id="assignment-language-seed" type="application/json">#rawJSON(assignmentLanguageJSON)</script>
<!-- Page facts for Public/assignment-new-page.js (loaded at the foot of
     the page): the draft ID (null before the first notebook upload) and
     whether a solution notebook exists yet. -->
<script id="new-assignment-config" type="application/json">{"draftID":#rawJSON(draftIDJSON),"hasSolutionNotebook":#if(solutionNotebook):true#else:false#endif}</script>
<script src="/authoring-language.js?v=#appVersion()"></script>

<!-- ── Notebook Check editor ────────────────────────────────────────────
     Now a body renderer of the unified Test Editor modal
     (test-editor-modal.js + test-renderer-check.js, wired near the bottom
     of the page).  The shell owns the chrome + type picker; the renderer
     builds its per-kind fields from the #check-schema seed.  No standalone
     overlay. -->

<!-- ── Pattern Family editor body ───────────────────────────────────────────
     A body renderer of the unified Test Editor modal (the shell owns the
     chrome).  Shared with assignment-edit via the _family-editor-body partial;
     the driving module (Public/pattern-family-editor.js) is shared too. -->
#extend("_family-editor-body")

<!-- Pattern family editor module — same implementation as the edit page,
     wired to draft-scoped URLs.  After a save, the page reloads so the
     server-rendered suite table picks up the newly generated scripts. -->
<script src="/pattern-family-editor.js?v=#appVersion()"></script>


<!-- ── Unified Suite Table ─────────────────────────────────────────────────
     The create page shares `suite-table.js` with the edit page; the two
     differ only in the URL wiring their page files pass in
     (Public/assignment-new-page.js here, Public/assignment-edit-page.js
     there).  All section CRUD goes through draft-scoped endpoints — see
     AssignmentRoutes+DraftSections.swift. -->
#if(draftID):
<script src="/accordion-row.js?v=#appVersion()"></script>
<script src="/suite-table.js?v=#appVersion()"></script>

<!-- Section inputs editor — the same shared modules the edit page loads
     (Public/inputs-editor-core.js + Public/section-inputs-editor.js).  Until
     v0.5.x this page carried its own pre-v0.4.160 inline copy, whose value
     parser had no `=` branch: a per-student expression typed into a section
     input was classified as a bare string and persisted as the literal text
     "= seed % 26", with the payload omitting `expressions` entirely.  The
     modules post to `form.action`, which the markup above already scopes to
     the draft endpoints, so there is no URL wiring to pass in. -->
<script src="/inputs-editor-core.js?v=#appVersion()"></script>
<script src="/section-inputs-editor.js?v=#appVersion()"></script>

<!-- Section header view/edit toggle, section delete, and section
     drag-reorder are all owned by suite-table.js, which this
     page and the edit page both load.  This page used to bind its own copies
     on top: the toggle/delete pair on `document` and the drag pair on the
     same #suite-sections element suite-table uses, so both fired.  Section
     delete raised two confirm dialogs, and the inline reorder handler read a
     `draftID` that was declared only inside a sibling IIFE — under strict
     mode a ReferenceError, so its POST never went out at all. -->
#endif

<!-- The Generate Starter Tests panel.  Loaded unconditionally, because its
     init (in assignment-new-page.js) runs whether or not a draft exists yet
     — with no draft it reports that one is needed. -->
<script src="/generated-starter-tests.js?v=#appVersion()"></script>

<!-- Notebook upload buttons, support files, the Generate Starter Tests
     panel init, the UW-date warning and the grading-mode hint are all
     wired by Public/assignment-new-page.js at the foot of the page. -->
<!-- ── Unified Test Editor modal (shell + renderers) ────────────────────────
     One modal for "+ Add Test"; picking a type morphs the body in place.
     The notebook-check form is a body renderer (test-renderer-check.js, fed
     by the #check-schema seed); family + script types delegate to their
     existing overlays during the staged rollout (the shell shows a
     "Continue →" button for those).  Renderer scripts load before the shell
     so they self-register first. -->
<script src="/test-renderer-check.js?v=#appVersion()"></script>
<script type="module" src="/test-renderer-script.js?v=#appVersion()"></script>
<script src="/test-editor-modal.js?v=#appVersion()"></script>
<script src="/support-files.js?v=#appVersion()"></script>
<script src="/assignment-new-page.js?v=#appVersion()"></script>
#endexport
#endextend
